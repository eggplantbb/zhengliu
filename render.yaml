services:
  - type: web
    name: maoxuan-distill
    runtime: node
    plan: free
    buildCommand: if [ ! -d vendor/MaoZeDongAnthology/src ]; then rm -rf vendor/MaoZeDongAnthology && git clone --depth 1 https://github.com/weiyinfu/MaoZeDongAnthology.git vendor/MaoZeDongAnthology; fi && npm install --include=optional && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
